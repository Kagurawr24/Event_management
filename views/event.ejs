<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Event Management</title>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Event Management</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/event">Add new event</a>
              </li> 
            </ul>
          </div>
        </div>
    </nav>
    
    <div class="container">
        <br>
        <div class="text-center"><h2>Event Details</h2></div>
        <br>
        <% if (event) { %> <!-- Check if event data exists -->
            <div>
                <p><b>Student Name:</b><br> <%= student.studentName %></p>
                <p><b>Student Image:</b><br> <img src = "<%= student.image %>" width="20%"></p>
                <p><b>Student DoB:</b><br> <%= student.dob %></p>
                <p><b>Student Contact:</b><br> $<%= student.contact %></p> 
            </div>
        <% } else { %>
            <p>No event found.</p>
        <% } %>
        <a href ="/">Back</a>
    </div>
    
</body>
</html>